# Plugin Template

A RemNote plugin template built with React.

## Usage

This plugin provides a sample widget in the sidebar and registers basic settings and commands.

## Architecture

Built with React 17 and Webpack, utilizing the `@remnote/plugin-sdk`.
